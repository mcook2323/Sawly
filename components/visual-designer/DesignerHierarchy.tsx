"use client";
import type { VisualScene } from "@/types/visualDesigner";

export function DesignerHierarchy({ scene, selectedId, onSelect, onVisibility, onLock }: { scene: VisualScene; selectedId: string | null; onSelect: (id: string) => void; onVisibility: (id: string) => void; onLock: (id: string) => void; }) {
  return <section aria-label="Scene hierarchy" className="h-full overflow-auto border-r border-[var(--color-border)] bg-[var(--color-surface)] p-4"><p className="ds-eyebrow">Scene</p><details open className="mt-3"><summary className="cursor-pointer font-semibold">{scene.projectName}</summary><ul className="mt-2 space-y-1">{scene.objects.map((object) => <li key={object.id} className={`rounded-xl border p-2 ${selectedId === object.id ? "border-[var(--color-brand)] bg-[var(--color-brand-soft)]" : "border-transparent"}`}><button type="button" onClick={() => onSelect(object.id)} className="w-full text-left"><span className="block text-sm font-semibold">{object.name}</span><span className="text-xs text-[var(--color-ink-muted)]">{object.type} · {object.status}{typeof object.metadata.editStatus === "string" ? ` · ${object.metadata.editStatus}` : ""}</span></button><div className="mt-2 flex gap-2"><button type="button" className="text-xs font-semibold underline" onClick={() => onVisibility(object.id)}>{object.visible ? "Hide" : "Show"}</button><button type="button" className="text-xs font-semibold underline disabled:cursor-not-allowed disabled:opacity-50" onClick={() => onLock(object.id)} disabled={object.status === "verified"}>{object.locked ? "Unlock" : "Lock"}</button></div></li>)}</ul></details></section>;
}
