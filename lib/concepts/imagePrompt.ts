import type { CustomConceptOption } from "@/types/customConcept";
export function buildConceptImagePrompt(concept: CustomConceptOption) { return `Photorealistic editorial home-improvement concept image of ${concept.title}. ${concept.description}. ${concept.environment} setting, ${concept.style} style, approximate proportions ${concept.approximateDimensions.width} wide by ${concept.approximateDimensions.depth} deep by ${concept.approximateDimensions.height} high. Materials: ${concept.suggestedMaterials.join(", ")}. Finish: ${concept.finishDirection}. Visible features: ${concept.majorFeatures.join(", ")}. Warm natural daylight, attainable premium home, realistic constructible proportions, natural materials, no people, no logos, no text, no labels, no dimensions, no blueprint annotations.`; }
